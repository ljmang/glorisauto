# Strapi 后端 API 根地址（不含 /api）
# 开发环境可指向本地或测试 Strapi
PUBLIC_STRAPI_URL=https://admin.glorisauto.com

# --- Cloudflare 开发环境 ---
# 本地用 Wrangler 时：npm run dev:cloudflare 会先 build 再以 wrangler pages dev 跑 dist
# 构建时需在环境里提供 PUBLIC_STRAPI_URL（或复制 .env 到 .dev.vars 供 wrangler 读取）
# Cloudflare Pages 部署时在 Dashboard → 项目 → Settings → Environment variables 配置同名变量
